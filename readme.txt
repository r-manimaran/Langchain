Projects using LangChain Framework and LLMs

> Used OpenAI
> Get your own openAI key and map it in .env file under OPENAI_API_KEY
> In the RAGApp, get the API key for Pinecone and environment for Pinecone

pip install -r requirements.txt